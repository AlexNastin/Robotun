<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns:context="http://www.springframework.org/schema/context"
	xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-4.0.xsd
                            http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context-4.0.xsd">


	<!-- For times when you need more complex processing, passing data to the 
		scheduled job -->
	<bean name="scheduleFullIndexSolrJob"
		class="org.springframework.scheduling.quartz.JobDetailFactoryBean">
		<property name="jobClass" value="by.robotun.webapp.quartz.job.ScheduleFullIndexSolrJob" />
		<property name="jobDataMap">
			<map>
				<entry key="synchronzationCSAHttpClient" value-ref="synchronzationCSAHttpClient" />
				<entry key="deserializationJSON" value-ref="deserializationJSON" />
				<entry key="serviceQuartz" value-ref="serviceQuartz" />
			</map>
		</property>
		<property name="durability" value="true" />
	</bean>


	<!-- Run the job every 5 seconds only on Weekends 0 0/30 7-21 ? * MON-SAT -->
	<bean id="cronTriggerSynchCSA"
		class="org.springframework.scheduling.quartz.CronTriggerFactoryBean">
		<property name="jobDetail" ref="jobDetailSynchCSA" />
		<property name="cronExpression" value="0/360 * * 1/1 * ? *" />
	</bean>


	<!-- Scheduler factory bean to glue together jobDetails and triggers to 
		Configure Quartz Scheduler -->
	<bean id="stdScheduler"
		class="org.springframework.scheduling.quartz.SchedulerFactoryBean">
		<property name="jobDetails">
			<list>
				<ref bean="jobDetailSynchCSA" />
			</list>
		</property>
		<property name="triggers">
			<list>
				<ref bean="cronTriggerSynchCSA" />
			</list>
		</property>
	</bean>

</beans>